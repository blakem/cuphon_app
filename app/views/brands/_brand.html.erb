<tr>
  <td class="brand">
	<%= brand.title %>
  </td>
  <td>
    <%= brand.merchant ? brand.merchant.name : "None" %>
  </td>
  <td>
	<%= brand.brand_aliases.map { |a| a.alias }.join('<br/>'.html_safe) %>
  </td>
  <td>
	<%= time_ago_in_words(brand.created_at) %> ago.
  </td>
  <td>
	<%= brand.subscriptions.count %>
  </td>
  <td>
	<%= brand.flags %>
  </td>
</tr>